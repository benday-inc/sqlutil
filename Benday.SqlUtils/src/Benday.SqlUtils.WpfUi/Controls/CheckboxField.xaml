<UserControl
    x:Class="Benday.SqlUtils.WpfUi.Controls.CheckboxField"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            x:Name="textBlock"
            Margin="6,4,4,4"
            Foreground="Red"
            Text="{Binding ValidationMessage}" 
            Visibility="{Binding IsValid, Converter={StaticResource NotVisibilityValueConverterInstance}}" />
        <StackPanel Grid.Row="1">
            <CheckBox IsChecked="{Binding Value, Mode=TwoWay}" Style="{StaticResource FieldCheckboxStyle}">
                <TextBlock
                    x:Name="_Label"
                    Style="{StaticResource FieldLabelStyle}"
                    Text="LABEL TEXT" />
            </CheckBox>
        </StackPanel>
    </Grid>

</UserControl>
